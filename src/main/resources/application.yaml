server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mvp_hotel_booking_db
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      format_sql: true